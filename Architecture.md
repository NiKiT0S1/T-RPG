# Техническое задание: Консольная RPG-игра

## Цель проекта
Создать консольную RPG-игру, которая использует все изученные темы: переменные, операторы, побитовые операции, условия, циклы и массивы.

---

## Функциональные требования

### 1. Персонаж игрока
**Переменные и типы данных:**
- `String playerName` - имя героя (ввод от пользователя)
- `int playerHealth` - текущее здоровье (начальное: 100)
- `int playerMaxHealth` - максимальное здоровье (начальное: 100)
- `int playerAttack` - сила атаки (начальное: 15)
- `int playerGold` - количество золота (начальное: 50)
- `int playerLevel` - уровень персонажа (начальное: 1)
- `int playerExp` - текущий опыт (начальное: 0)

**Побитовые операторы - система статусов:**
- `byte playerStatus` - флаги состояний (начальное: 0b0000)
  - `0b0001` - отравлен (теряет здоровье каждый ход)
  - `0b0010` - усилен (следующая атака наносит +50% урона)
  - `0b0100` - защищается (получает -50% урона)

### 2. Враги
**Массивы:**
- Двумерный массив `int[][] enemies` с характеристиками врагов:
  - Столбцы: [здоровье, атака, награда_золота, награда_опыта]
  - Минимум 4 типа врагов разной сложности
- Массив `String[] enemyNames` с именами врагов

Примеры врагов:
- Слабый (30 HP, 8 ATK)
- Средний (50 HP, 12 ATK)
- Сильный (80 HP, 18 ATK)
- Босс (120 HP, 25 ATK)

### 3. Инвентарь
**Массивы:**
- `int[] inventory` - количество каждого предмета
  - `inventory[0]` - зелья здоровья (восстанавливают HP)
  - `inventory[1]` - зелья силы (дают статус "усилен")
  - `inventory[2]` - зелья защиты (дают статус "защита")

Начальное состояние: 2 зелья здоровья

---

## Игровые механики

### Главное меню (Цикл while)
Варианты действий:
1. Искать врага (начать бой)
2. Открыть инвентарь
3. Посетить магазин
4. Посмотреть статус персонажа
5. Отдохнуть (восстановить здоровье за золото)
0. Выход из игры

### Боевая система (Вложенный цикл while)

**Начало боя:**
- Случайный выбор врага из массива (Random)
- Отображение информации о враге

**Действия в бою (Условные операторы):**
1. **Атаковать:**
   - Базовый урон = `playerAttack + случайное(0-5)`
   - Если активен статус "усилен": урон × 1.5, снять статус
   - Враг теряет здоровье

2. **Защищаться:**
   - Установить флаг защиты побитовой операцией `|=`
   - Следующая атака врага наносит 50% урона

3. **Использовать предмет:**
   - Выбор предмета из инвентаря
   - Уменьшить количество предмета на 1
   - Применить эффект

4. **Бежать:**
   - Выйти из боя без награды

**Ход врага (Основные операторы):**
- Урон врага = `enemyAttack + случайное(0-5)`
- Если игрок защищается: урон / 2
- Проверка статуса "отравлен": дополнительный урон

**Условия окончания боя:**
- Здоровье врага ≤ 0 → победа (получить золото и опыт)
- Здоровье игрока ≤ 0 → поражение (конец игры)
- Игрок сбежал → выход в главное меню

### Система прогрессии

**Повышение уровня (Условные операторы):**
- Требуемый опыт = `уровень × 100`
- При достижении: уровень +1, сброс опыта
- Бонусы: max HP +20, атака +5, полное восстановление здоровья

### Магазин (Switch-case)
Товары и цены:
- Зелье здоровья: 20 золота
- Зелье силы: 30 золота  
- Зелье защиты: 25 золота

Проверка наличия золота перед покупкой

### Отдых
- Стоимость = `(maxHealth - currentHealth) / 2` (минимум 10)
- Восстановление здоровья до максимума
- Сброс всех статусов

---

## Структура программы

```
1. Импорты (Scanner, Random)

2. Объявление всех переменных
   - Характеристики игрока
   - Константы для побитовых флагов
   - Массивы врагов
   - Массив инвентаря

3. Приветствие и ввод имени

4. ГЛАВНЫЙ ИГРОВОЙ ЦИКЛ (while gameRunning)
   │
   ├─ Отображение меню и характеристик
   ├─ Ввод выбора
   │
   └─ SWITCH по выбору:
      │
      ├─ case 1: БОЙ
      │   ├─ Выбор случайного врага
      │   └─ ЦИКЛ БОЯ (while inBattle)
      │       ├─ Отображение состояния
      │       ├─ Выбор действия игрока
      │       ├─ IF-ELSE для действий
      │       ├─ Проверка победы над врагом
      │       ├─ Ход врага
      │       ├─ Проверка побитовых статусов
      │       └─ Проверка поражения игрока
      │
      ├─ case 2: ИНВЕНТАРЬ
      │   └─ FOR цикл для отображения предметов
      │
      ├─ case 3: МАГАЗИН
      │   └─ IF проверка золота и покупка
      │
      ├─ case 4: СТАТУС
      │   └─ Вывод всех характеристик
      │   └─ Проверка побитовых флагов (& оператор)
      │
      ├─ case 5: ОТДЫХ
      │   └─ Расчет стоимости и восстановление
      │
      └─ case 0: ВЫХОД

5. Проверка причины завершения игры
6. Закрытие Scanner
```

---

## Требования к использованию тем

### ✅ Переменные и типы данных
- [ ] int для числовых характеристик
- [ ] String для имен
- [ ] boolean для флагов состояния игры
- [ ] byte для побитовых флагов
- [ ] double (опционально) для расчетов

### ✅ Основные операторы
- [ ] Арифметические: +, -, *, /, % (расчет урона, опыта, цен)
- [ ] Присваивание: =, +=, -=, *=, /=
- [ ] Сравнение: ==, !=, <, >, <=, >= (проверки здоровья, золота)
- [ ] Логические: &&, ||, ! (составные условия)
- [ ] Тернарный: ? : (расчет минимальной стоимости)

### ✅ Побитовые операторы
- [ ] | (установка флага статуса)
- [ ] & (проверка флага статуса)  
- [ ] ~ (инверсия для сброса)
- [ ] |= (добавление статуса)
- [ ] &= с ~ (удаление статуса)

### ✅ Условные операторы
- [ ] if-else (проверки в бою, покупки)
- [ ] else if (множественные условия)
- [ ] switch-case (меню, действия)
- [ ] Вложенные условия

### ✅ Циклы
- [ ] while (главный цикл игры, цикл боя)
- [ ] for (перебор массивов инвентаря/врагов)
- [ ] Вложенные циклы
- [ ] break и continue

### ✅ Массивы
- [ ] Одномерные массивы (инвентарь, имена)
- [ ] Двумерные массивы (характеристики врагов)
- [ ] Чтение из массива
- [ ] Запись в массив
- [ ] Обход массива циклом

---

## Рекомендации по разработке

### Этап 1: Базовая структура
1. Создайте все переменные
2. Реализуйте главное меню с switch
3. Добавьте условие выхода из игры

### Этап 2: Боевая система
1. Создайте массивы врагов
2. Реализуйте цикл боя
3. Добавьте базовые атаки (без статусов)
4. Проверьте условия победы/поражения

### Этап 3: Побитовые статусы
1. Определите константы для флагов
2. Реализуйте установку статусов
3. Добавьте проверки статусов в бою
4. Реализуйте сброс статусов

### Этап 4: Инвентарь и магазин
1. Создайте массив инвентаря
2. Реализуйте использование предметов
3. Добавьте магазин с покупками

### Этап 5: Прогрессия
1. Добавьте систему опыта
2. Реализуйте повышение уровня
3. Добавьте отдых

### Этап 6: Полировка
1. Улучшите вывод информации
2. Добавьте случайность в урон
3. Балансируйте сложность
4. Протестируйте все механики

---

## Подсказки для реализации

**Работа с побитовыми флагами:**
```java
// Установка флага
playerStatus |= POISONED;

// Проверка флага
if ((playerStatus & POISONED) != 0) {
    // игрок отравлен
}

// Снятие флага
playerStatus &= ~POISONED;

// Сброс всех флагов
playerStatus = 0;
```

**Случайный выбор из массива:**
```java
int index = random.nextInt(массив.length);
```

**Расчет урона с вариацией:**
```java
int damage = базовыйУрон + random.nextInt(максимальнаяВариация);
```

---

## Критерии успеха

Ваш проект готов, если:
- ✅ Использованы ВСЕ изученные темы
- ✅ Игра работает без ошибок
- ✅ Можно выиграть и проиграть
- ✅ Все механики работают логично
- ✅ Код структурирован и понятен

## Бонусные задачи (опционально)

1. Добавьте критический удар (случайный шанс удвоить урон)
2. Реализуйте систему редкости врагов
3. Добавьте историю последних 5 действий (массив String)
4. Создайте "режим арены" - бой с волнами врагов
5. Добавьте достижения (побить 10 врагов, достичь 5 уровня и т.д.)

---

**Удачи в разработке! Если возникнут вопросы по реализации какой-то части - спрашивайте! 🎮**